#!/bin/bash

function select_bin {
  BIN_CMD="$1"
  platform=`uname -i` 
  case $platform in
    x86_64)
      plat_abi=x86_64-linux-gnu
      BIN_CMD="${BIN_CMD}.x86_64"
      ;;
    armv7l)
      plat_abi=arm-linux-gnueabihf
      BIN_CMD="${BIN_CMD}.armhf"
      ;;
    *)
      echo "unknown platform for snappy-magic: $platform. remember to file a bug or better yet: fix it :)"
      ;;
  esac
  echo ${BIN_CMD}
}
